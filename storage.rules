rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Allow read access on all files
    allow read;
    
    // Allow write access for any authenticated user on any file
    allow write: if request.auth != null;

  }
}
